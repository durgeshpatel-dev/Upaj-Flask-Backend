# Core Flask dependencies
flask==3.0.3
flask-cors==5.0.0
gunicorn==21.2.0

# Optional ML dependencies (will fallback to mock if not available)
pandas>=2.0.0; python_version>="3.8"
numpy>=1.21.0; python_version>="3.8"
scikit-learn>=1.0.0; python_version>="3.8"
joblib>=1.0.0; python_version>="3.8"

# Image processing (optional)
pillow>=9.0.0; python_version>="3.8"

# TensorFlow (optional, will use mock if fails)
tensorflow>=2.12.0; python_version>="3.8" and python_version<="3.11"