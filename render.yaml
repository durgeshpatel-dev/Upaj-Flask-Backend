version: 2
build:
  commands:
    - pip install --upgrade pip
    - pip install -r requirements_production.txt || pip install flask flask-cors gunicorn
start:
  command: gunicorn app_production:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --max-requests 1000